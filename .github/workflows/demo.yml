name: my-first-flow
on:
  push:
    branches: [main]
    workflow-dispatch:
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - run echo "Hello World!"
      - uses: actions/checkout@v2
      - run: ls
        #- docker login -u weichen66 -pass Cw1010sktc10
      - run: echo "Goodbye!"



